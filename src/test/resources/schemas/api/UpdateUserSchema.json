{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Update User Response Schema",
  "description": "Schema for a successful user update response from DummyAPI",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique ID of the updated user"
    },
    "firstName": {
      "type": "string",
      "description": "Updated first name of the user"
    },
    "lastName": {
      "type": "string",
      "description": "Updated last name of the user"
    },
    "updatedDate": {
      "type": "string",
      "format": "date-time",
      "description": "Last updated date of the user (ISO 8601 format)"
    }
  },
  "required": [
    "id",
    "firstName",
    "lastName",
    "updatedDate"
  ],
  "additionalProperties": true
}